const start = () => {
    let count = 1
    while(count <= 10){
        console.log(count)
        count = count + 1
    }                                        //ele vai passar por tudo isso e vai fazer 10x
}

start()